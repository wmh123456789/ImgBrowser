使用PyQt开发一个图片浏览器。可以实现在图片上叠加一些关键点，同时可以查看关键点的信息。
图片浏览器的界面设计是这样的：
左侧是一个文件列表，显示选定目录下所有的JPG或PNG文件的名字。
上部有菜单栏，菜单中包括打开文件和打开文件夹的选项。打开后的文件显示在文件列表中。
中间显示图片，默认缩放到高600*宽800。
右侧是一个工具栏，可以配置一些按钮，复选框，文字输入框等等控件。
其中，按钮包含缩小、放大、自动三个按钮，分别可以控制图片进行缩小、放大、缩放到适合窗口。
下侧有一个状态栏显示一些状态信息。

请进行如下修改：
1. 将文件列表和图片显示区域设成宽度可调。
2. 目前zoom in 和 zoom out两个按钮并不能实现图片的放大缩小，请解决这个问题。

目前缩放相关的三个函数是不是针对原始图片进行的缩放。导致显示的效果不够清晰。请修改这三个函数。

请给文件列表和图片显示区设计一个合适的初始size。文件列表宽度适合显示12个字符的文件名。图片显示区适合高600*宽800的图片。

为了实现两张图片同时显示并进行对比，请基于现有的布局方式，在现有的文件列表、图片显示区、工具栏（这三个功能区称为一套控件）的下方，再布局一套控件。
上下两套控件共享同一个图片文件夹（两个文件列表对应的是同一个文件夹），可以独立的选择各自文件列表中的图片并在显示区显示。
两个图片可以独立的进行缩放操作。
请给出代码修改方案。


